name: foodradar
description: App to scan food for nutrients and allergens
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  http: ^0.13.5
  flutter_native_image: ^0.0.6+1
  camera: ^0.10.2
  image_gallery_saver: ^1.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
#  assets:
